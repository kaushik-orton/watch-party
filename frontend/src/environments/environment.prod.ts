export const environment = {
  production: true,
  // TODO: Replace this with your actual deployed backend URL (e.g. from Railway or Render)
  signalingServerUrl: 'https://your-production-backend-url.com'
};
